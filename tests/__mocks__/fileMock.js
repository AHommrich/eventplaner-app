// Static-asset stub: any `require('./eve-logo.png')` in a test resolves to
// this string instead of going through the Metro asset pipeline (which does
// not exist under Jest). Referenced by `jest.config.js` moduleNameMapper.
module.exports = 'test-file-stub';
